name: cat-appservice-vnet
metadata:
  template: cat-appservice-vnet@1.0.0
services:
  web:
    project: .
    language: py
    host: appservice
    resourceGroup: ${AZURE_RESOURCE_GROUP}
infra:
  layers:
    - name: network
      path: infra/network
    - name: main
      path: infra